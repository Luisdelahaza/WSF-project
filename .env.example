# TiTiler API base URL.
# Staging (simplest — no local Docker, public read, CORS open): use as-is.
# Local Docker alternative: http://localhost:8000
NEXT_PUBLIC_TITILER_URL=https://d3nvi0df2mtlx1.cloudfront.net

# WSF dataset (staging).
NEXT_PUBLIC_WSF_DATASET_URL=s3://wsf-platform/data/layers/World_WSF_20160701-20260101.zarr

# Endpoint family for the dataset above: geozarr | md | cog
NEXT_PUBLIC_WSF_API_PREFIX=geozarr

# Variable name in the dataset
NEXT_PUBLIC_WSF_VARIABLE=wsf_tracker
